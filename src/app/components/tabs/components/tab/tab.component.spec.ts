import { TabComponent } from './tab.component';

describe('TabComponent', () => {
  let component: TabComponent;

  beforeEach(() => {
    component = new TabComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
