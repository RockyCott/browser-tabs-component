import { TabsFullComponent } from './tabs-full.component';

describe('TabsFullComponent', () => {
  let component: TabsFullComponent;

  beforeEach(() => {
    component = new TabsFullComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
